package com.aws.codestar.projecttemplates.service;





public class ShoppingServiceTest {
/*
    @Autowired
    private ShoppingService shoppingService;

    @Autowired
    private CustomerService customerService;
    Customer customer;

    @Autowired
    private UrunService urunService;


    @Before
    public void setUp() throws Exception {
        customer =customerService.findById((long) 1);
    }

    @Test
    public void saveTest(){

        List<Urun> urunList = new ArrayList<>();
        urunList.add(urunService.getUrun((long) 1));
        urunList.add(urunService.getUrun((long) 2));

        Shopping shopping = new Shopping();
        shopping.setCustomer(customer);
        shopping.setUrunList(urunList);
        shoppingService.save(shopping);
        List<Shopping> returnShopping = shoppingService.getAllShopping();

        Shopping shopping1 = new Shopping();
        shopping1.setUrunList(urunList);
        shopping1.setCustomer(customer);

        shoppingService.save(shopping1);

        assertEquals(2,returnShopping.size());

        assertEquals(String.valueOf(1),shoppingService.getLasShopping().getId().toString());

        Customer customer1 = returnShopping.get(0).getCustomer();


        //assertEquals(returnShopping.iterator().next().getUrunList().size(),2);


    }
    */

}